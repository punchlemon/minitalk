#ifndef LIBFT_H
# define LIBFT_H

# include <stdlib.h>
# include <limits.h>

int		ft_atoi(const char *str);
char	*ft_itoa(int n);
size_t	ft_strlen(const char *s);
char	*ft_strdup(const char *s1);

#endif